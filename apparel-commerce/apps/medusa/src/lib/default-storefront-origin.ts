/**
 * Fallback storefront origin when env is unset. Keep in sync with
 * `packages/sdk/src/public-site-url.ts` (DEFAULT_PUBLIC_SITE_ORIGIN).
 */
export const DEFAULT_STOREFRONT_ORIGIN =
  "https://maharlika-apparel-custom.vercel.app";

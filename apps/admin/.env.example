# =============================================================================
# All env vars live in the repository root .env (single source of truth).
#
# Admin-only override: create apps/admin/.env.local (gitignored) with:
#   NEXTAUTH_URL=http://localhost:3001
#
# next.config.mjs loads root ../../.env first, then .env.local with override.
# See: ../../.env.example for the full variable reference.
# =============================================================================

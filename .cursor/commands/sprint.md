/sprint

I am the Product Owner Master.
You are not the scope owner.
You are not allowed to reinterpret my backlog.

My listed items are mandatory sprint commitments.
You must treat them as fixed scope.

Forbidden actions:
- marking my items optional
- turning implementation into recommendation
- moving items to future phases
- reducing acceptance criteria
- replacing build work with docs
- inventing substitute scope
- silently dropping items

Required behavior:
- keep all my items in Committed
- mark an item Blocked only when there is a concrete blocker
- state the blocker precisely
- continue planning around the blocker without removing the item
- put only your own new ideas in Future Considerations

Execution rules:
1. Every item I listed is COMMITTED exactly as written.
2. Do not move any user-provided item into Optional, Recommended, Later, Stretch, Nice-to-have, or Future.
3. Do not split my requirements into “core” and “non-core” unless I explicitly instruct you to.
4. Do not replace implementation with documentation, placeholders, env vars, mocks, or design notes.
5. Do not rewrite acceptance criteria.
6. Do not delete items.
7. Do not de-scope items.
8. Do not move items to another phase or sprint unless I explicitly approve it.
9. Any new ideas you introduce must go into Future Considerations only.
10. If an item cannot currently be completed, keep it in COMMITTED and mark it BLOCKED with the exact blocker.

Re-generate the sprint plan now using only these columns:

- Committed
- Blocked
- Future Considerations

Strict rules for output:
- All user-provided items must appear under Committed unless there is a real blocker.
- Blocked items remain sprint commitments; they are not removed from scope.
- Future Considerations may contain only ideas that did not come from me.
- Do not add Optional, Recommended, Nice-to-have, Later, Stretch, Phase 2, or similar columns.
- Do not justify scope reduction.
- Do not ask for permission to downgrade scope.
- Do not summarize why items are hard; plan the execution.

For each committed item, include:
- exact deliverable
- owner
- files/services affected
- dependencies
- proof of completion

Output format:
1. Committed
2. Blocked
3. Future Considerations

Nothing else.
Regenerate the sprint now.

# Root .env — required for storefront, admin, Express API, and Medusa.
# Copy to .env and set every variable. Use apps/admin/.env.local for NEXTAUTH_URL=http://localhost:3001 when admin differs from storefront.
# Production: replace http://localhost:* in site URLs and CORS lists with your public HTTPS origins.
# Generate secrets: openssl rand -base64 32 (JWT/COOKIE/NEXTAUTH/TRACKING) or openssl rand -hex 32 (INTERNAL_API_KEY, CHANNEL_WEBHOOK).

# =============================================================================
# 1. App URLs
# =============================================================================
NEXT_PUBLIC_SITE_URL=http://localhost:3000
PUBLIC_STOREFRONT_URL=http://localhost:3000
API_URL=http://localhost:4000

# =============================================================================
# 2. NextAuth
# =============================================================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# =============================================================================
# 3. Google OAuth
# =============================================================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# =============================================================================
# 4. Supabase (platform DB, RBAC, CMS, payment_connections BYOK)
# =============================================================================
LEGACY_DATABASE_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_IMAGE_HOSTNAMES=**.supabase.co

# =============================================================================
# 5. Express internal API
# =============================================================================
INTERNAL_API_KEY=

# =============================================================================
# 6. Express CORS
# =============================================================================
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# =============================================================================
# 7. GDPR retention
# =============================================================================
DATA_RETENTION_DAYS=730

# =============================================================================
# 8. Admin
# =============================================================================
ADMIN_ALLOWED_EMAILS=
NEXT_PUBLIC_STAFF_RBAC_STRICT=true
CHANNEL_WEBHOOK_SECRET=

# =============================================================================
# 9. Medusa client (storefront and admin)
# =============================================================================
NEXT_PUBLIC_MEDUSA_URL=http://localhost:9000
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=
NEXT_PUBLIC_MEDUSA_REGION_ID=
NEXT_PUBLIC_MEDUSA_SALES_CHANNEL_ID=
NEXT_PUBLIC_MEDUSA_PAYMENT_PROVIDER_ID=pp_stripe_stripe

MEDUSA_BACKEND_URL=http://localhost:9000
MEDUSA_PUBLISHABLE_API_KEY=
MEDUSA_REGION_ID=
MEDUSA_SALES_CHANNEL_ID=
MEDUSA_SECRET_API_KEY=

# =============================================================================
# 10. Medusa server
# =============================================================================
JWT_SECRET=
COOKIE_SECRET=
DATABASE_URL=
DB_NAME=medusa-v2

# =============================================================================
# 11. Medusa CORS
# =============================================================================
STORE_CORS=http://localhost:3000
ADMIN_CORS=http://localhost:3001,http://localhost:9000
AUTH_CORS=http://localhost:3001,http://localhost:9000
STOREFRONT_PUBLIC_URL=http://localhost:3000

# =============================================================================
# 12. Payment BYOK (Medusa loads provider secrets from payment_connections)
# =============================================================================
PAYMENT_CREDENTIALS_SOURCE=platform
PAYMENT_CONNECTIONS_ENCRYPTION_KEY=

# =============================================================================
# 13. Resend and signed tracking links (Medusa requires TRACKING_HMAC_SECRET when Resend is set in production)
# =============================================================================
RESEND_API_KEY=
RESEND_FROM_EMAIL=
TRACKING_HMAC_SECRET=
